import { G as GenericWorker_1 } from './stream/GenericWorker.js';
import { f as flate } from './flate.js';

var STORE = {
    magic: "\x00\x00",
    compressWorker : function (compressionOptions) {
        return new GenericWorker_1("STORE compression");
    },
    uncompressWorker : function () {
        return new GenericWorker_1("STORE decompression");
    }
};
var DEFLATE = flate;

var compressions = {
	STORE: STORE,
	DEFLATE: DEFLATE
};

export { compressions as c };
